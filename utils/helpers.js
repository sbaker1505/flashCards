import React from 'react'
import { AsyncStorage } from 'react-native'

export getDecks() {
  // returns all decks with titles, answers & questions
}

export getDeck(id) {
  // return deck with id
}

export saveDeckTitle(title) {
  // adds title to deck
}

export addCardToDeck(title, card) {
  // add card to list of questions
}
